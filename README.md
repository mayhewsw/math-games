# Pygame Zero Math Games

Some super simple math games written in [Pygame Zero](https://pygame-zero.readthedocs.io/en/stable/). The `alien` and `eep` images and sounds are not mine.

To get started:
```
> pip install pgzero
> pgzrun asteroids.py
```

